```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Flipbook Invitation</title>

<style>
  body {
    margin: 0;
    background: #e5e5e5;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
  }

  .book {
    position: relative;
    width: 90vw;
    max-width: 400px;
    height: 70vh;
    max-height: 600px;
    perspective: 1500px;
    background: linear-gradient(to right, #ddd, #bbb);
    border-radius: 12px;
  }

  .page {
    position: absolute;
    width: 100%;
    height: 100%;
    transform-origin: left;
    transition: transform 0.8s ease;
    cursor: pointer;
    box-shadow: 0 10px 20px rgba(0,0,0,0.15);
  }

  .page img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 10px;
    backface-visibility: hidden;
  }

  .page.flipped {
    transform: rotateY(-180deg);
    box-shadow: -10px 10px 20px rgba(0,0,0,0.2);
  }

  .page:nth-child(1) { z-index: 5; }
  .page:nth-child(2) { z-index: 4; }
  .page:nth-child(3) { z-index: 3; }
  .page:nth-child(4) { z-index: 2; }
  .page:nth-child(5) { z-index: 1; }

  .page-number {
    position: absolute;
    bottom: 10px;
    right: 15px;
    font-size: 14px;
    background: rgba(255,255,255,0.6);
    padding: 2px 6px;
    border-radius: 5px;
  }

  .controls {
    margin-top: 15px;
  }

  button {
    padding: 8px 14px;
    margin: 5px;
    border: none;
    border-radius: 6px;
    background: #333;
    color: white;
    cursor: pointer;
  }

  button:hover {
    background: #555;
  }
</style>
</head>

<body>

<div class="book">
  <div class="page"><img src="page1.jpg"><span class="page-number">1</span></div>
  <div class="page"><img src="page2.jpg"><span class="page-number">2</span></div>
  <div class="page"><img src="page3.jpg"><span class="page-number">3</span></div>
  <div class="page"><img src="page4.jpg"><span class="page-number">4</span></div>
  <div class="page"><img src="page5.jpg"><span class="page-number">5</span></div>
</div>

<div class="controls">
  <button onclick="prevPage()">⬅ Prev</button>
  <button onclick="nextPage()">Next ➡</button>
</div>

<script>
  const pages = document.querySelectorAll(".page");
  let current = 0;

  function nextPage() {
    if (current < pages.length) {
      pages[current].classList.add("flipped");
      current++;
    }
  }

  function prevPage() {
    if (current > 0) {
      current--;
      pages[current].classList.remove("flipped");
    }
  }

  // CLICK support
  document.addEventListener("click", (e) => {
    if (e.target.tagName === "IMG") {
      nextPage();
    }
  });

  // KEYBOARD support
  document.addEventListener("keydown", (e) => {
    if (e.key === "ArrowRight") nextPage();
    if (e.key === "ArrowLeft") prevPage();
  });

  // SWIPE support
  let startX = 0;

  document.addEventListener("touchstart", e => {
    startX = e.touches[0].clientX;
  });

  document.addEventListener("touchend", e => {
    let endX = e.changedTouches[0].clientX;

    if (startX - endX > 50) nextPage();
    if (endX - startX > 50) prevPage();
  });
</script>

</body>
</html>
```
